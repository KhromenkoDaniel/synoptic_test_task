import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    marginBottom: 64,
    marginTop: 0,
  },
  calendarDays: {
    flexDirection: 'row',
    alignItems: 'center',
    marginHorizontal: 20,
    marginBottom: 10,
  },
  calendarDaysText: {
    color: '#fff',
    fontSize: 18,
    fontWeight: '500',
    marginLeft: 4,
  },
});
